# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# ===============================================
# Configuración global de Node.js y Proyectos JS
# ===============================================

# Dependencias del Módulo
/node_modules
/package-lock.json  # A veces se ignora, pero es útil para CI/CD
/npm-debug.log
/yarn-error.log
/pnpm-debug.log

# Archivos de caché y temporales
.DS_Store
.env
.env.local
.env.development.local
.env.production.local

# Archivos de logs
logs
*.log
npm-debug.log*
yarn-debug.log*

# ===============================================
# SECCIÓN DE BACKEND (API, Orchestrator, Gateway)
# ===============================================

# No hay archivos específicos grandes aquí, ya que son scripts .js.
# Si tuvieras una carpeta para logs de los servicios, ignórala:
/backend/logs/

# Si usas algún IDE como VSCode, ignora su configuración local:
.vscode/

# ===============================================
# SECCIÓN DE FRONTEND (Vite/React/Tailwind)
# ===============================================

# Directorio de salida de la compilación de Vite
# Normalmente es 'dist'
/dist
/build

# Archivos de caché de Vite
.vite/

# Si usas algún archivo de configuración sensible en el frontend
# (Aunque las variables públicas deben ir en NEXT_PUBLIC_..., etc.)
.env.*

# ===============================================
# SECCIÓN DE DOCKER
# ===============================================

# Evita subir imágenes de Docker y volúmenes locales
# Aunque docker-compose.yml es necesario, estos no:
*.swp
*.bak
*.swo

# Ignorar archivos generados por Docker (logs/volumes)
# Si tu sistema de logs guarda datos dentro de una carpeta del repo:
/docker/data/